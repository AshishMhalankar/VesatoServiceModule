<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vesatago.userapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".helper.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".modules.authentication.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".modules.authentication.LoginActivity" /> -->
        <activity android:name=".modules.authentication.RegistrationActivity" />
        <activity android:name=".modules.SplashActivity" />
        <activity android:name=".modules.authentication.OtpVerificationAtivity" />
        <activity android:name=".modules.MainActivity" />
        <activity android:name=".modules.vehicle.VehicleHireActivity" />
        <activity android:name=".modules.vehicle.ThankYouActivity"></activity>
    </application>

</manifest>